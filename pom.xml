<project>
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.hegemonyrpg</groupId>
    <artifactId>servett-parent</artifactId>
    <version>dev-SNAPSHOT</version>
    <packaging>pom</packaging>
    <name>Servett (Parent)</name>

    <build>
        <defaultGoal>install</defaultGoal>
        <finalName>${project.artifactId}</finalName>
    </build>
    <modules>
        <module>Servett-API</module>
        <module>Servett-Server</module>
    </modules>
</project>
